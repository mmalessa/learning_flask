# Learning Flask

## First steps
```sh
make build
make up
make console
```

```sh
curl -sSL https://install.python-poetry.org | python3 -
poetry init
pip install Flask
poetry add Flask
```

Run application
```sh
make run
```